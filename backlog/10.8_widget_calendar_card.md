# Widget — Calendar Card (Mini)

## Scope
- Shared embedded mini calendar (`DoseCalendarWidget` in mini variant).
- Focus: selection behavior, day-view drill-in, and displaying dose cards when a day is selected.

## Requests
- [x] When a day is selected, display dose cards for that day (Home backlog request).
- [x] Ensure the mini calendar selection state is obvious (selected day styling).
- [x] Provide a quick “Open full calendar” action.

## Recommendations
- [x] Extract a shared `CalendarCard` wrapper around the mini calendar for reuse across screens.
- [ ] Reuse the same selected-day dose-card stage behavior between mini and full calendar variants where possible.
- [ ] Add a compact empty state for days with no doses.

## Notes
- Source backlog item: `backlog/2_home.md` → “Calendar: Needs to display the dose cards when a day is selected.”

## Links
- Home screen: `lib/src/features/home/presentation/home_page.dart`
- Card widget: `lib/src/widgets/cards/calendar_card.dart`
- Calendar widget: `lib/src/widgets/calendar/dose_calendar_widget.dart`
